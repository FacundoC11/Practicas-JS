const valor1 = prompt("Ingrese un valor");
const valor2 = prompt("Ingrese un segundo valor");
if (valor1 > valor2) {
  document.write(`${valor1} es mayor a ${valor2}`);
} else {
  document.write(`${valor1} es menor a ${valor2}`);
}
