const divisible = parseInt(prompt("Ingrese un valor"))
if (divisible % 2 === 0){document.write(`el numero ${divisible} es divisible por 2`)}
else { document.write(`el numero ${divisible} no es divisible por 2`)
}



